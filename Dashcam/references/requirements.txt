# Python Requirements for Frame Extraction Skill
# No external Python packages required - uses only standard library

# Standard library modules used:
# - subprocess (video processing with ffmpeg)
# - pathlib (file path handling)
# - json (stats output)
# - shutil (file operations)
# - sys (exit codes, arguments)

# System Requirements (not pip installable):
# - ffmpeg with CUDA support
# - NVIDIA GPU with drivers
# - CUDA toolkit
# - Python 3.7+

# To verify system requirements:
# ffmpeg -hwaccels | grep cuda
# nvidia-smi
# python3 --version
